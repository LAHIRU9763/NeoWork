package com.alaqahtml5.qa.pages;

import org.apache.logging.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class FAQPage extends BasePageObject {
	public FAQPage(WebDriver driver, Logger log) {
		super(driver, log);
	}
}
